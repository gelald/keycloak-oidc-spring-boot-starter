# Keycloak OIDC Spring Boot Starter

## Worktree 规范

- Worktree 目录统一使用项目根目录下的 `.worktrees/`
- **禁止使用** Claude Code 内置的 `EnterWorktree` 工具（它会使用 `.claude/worktrees/`）
- 创建 worktree 时，必须使用 `superpowers:using-git-worktrees` skill
